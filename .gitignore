# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
# NOTE: notebooks/*.ipynb IS committed for submission (with output cells preserved
# for grading). The Jupytext `.py` files are the source-of-truth template;
# `.ipynb` is the student's executed deliverable.

# Lab artifacts (regenerated on every run; do not commit)
data/pref/
data/eval_prompts.json
adapters/sft-mini/
adapters/dpo/
adapters/orpo/
adapters/merged-fp16/
gguf/
*.gguf
runs/
wandb/
unsloth_compiled_cache/

# Local (non-Colab) run of the combined notebook from inside colab/ — same
# regenerated artifacts as above, just rooted one level down because that's
# where REPO_ROOT resolved to for this run.
colab/adapters/
colab/data/
colab/notebooks/
colab/submission/
colab/unsloth_compiled_cache/

# HuggingFace cache (Colab + local — model weights are huge)
.cache/
hf_cache/

# Env / secrets
.env
.env.local
*.token

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
